# Forma — агенттерге арналған жедел нұсқаулық

Толық спецификация — `CLAUDE.md`. Мұнда тек кодтауға кіріспес бұрын білуі
керек нәрселер.

## Expo нұсқасы

SDK 57 (RN 0.86, React 19.2). Код жазбас бұрын нақты нұсқаның құжаттамасын
оқыңыз: https://docs.expo.dev/versions/v57.0.0/

SDK 56+ ережесі: `@react-navigation/*` пакеттерінен ТІКЕЛЕЙ импорттауға
болмайды — сәйкес `expo-router` кіру нүктелерін қолданыңыз.

## Шешілген мәселелер — қайта «түзетпеңіз»

**Шрифт.** CLAUDE.md §2-де Unbounded пен Manrope аталған, бірақ екеуі де
қазақ әріптерін көтермейді (cmap тексерілген: Unbounded 14 әріпті,
Manrope 8 әріп + ₸ таңбасын білмейді). Ауыстырылды: **Onest** (мәтін) +
**Montserrat** (сан, тақырып). Caveat өзгермеді. Тексеру:
`npm run check:glyphs -- node_modules/@expo-google-fonts/onest/400Regular`

**`.npmrc` → `legacy-peer-deps=true`.** expo-router өзінің веб бөлігі үшін
`react-dom@19.2.8`-ті тартады, ал SDK 57 `react@19.2.3`-ті бекітеді.
`npx expo install --check` ағашты дұрыс деп санайды. React нұсқасын
көтеріп «түзетуге» тырыспаңыз.

**`react-native-reanimated` + `react-native-worklets`.** CLAUDE.md §2-де
жоқ, бірақ §7.1 (календарьдың жиырылуы) мен §7.3 (период ауыстырғышы)
дәл сол анимацияларды талап етеді. Reanimated 4 worklets пакетін бөлек
қажет етеді.

**`babel-preset-expo`** — devDependency ретінде тұр. Жойылса Metro
іске қосылмайды.

## Кодтау ережелері

- Қазақ мәтіні тек `i18n/kk.ts` ішінде. Компонентте қатқыл жазылмайды.
- Түс, радиус, көлеңке тек `theme/tokens.ts` ішінен. Ойдан мән қоспаңыз —
  әр мән `design/*.dc.html` макетінен көшірілген.
- Эмодзи ЖОҚ. Иконка — `components/icons/`, react-native-svg, stroke-based.
- Прогресс, pace, серия есептері — `lib/` ішінде таза функция, тесті бар.
- Файл 300 жолдан асса — бөліңіз.
- `any` жоқ, `strict: true`.

## Ең маңызды екі ереже (бұзылса қосымшаның мәні кетеді)

1. **Әдеттер (`habit_logs`) пайызға МҮЛДЕ кірмейді.** Күндік сақина тек
   тапсырмалардан есептеледі.
2. **«Керек еді» мәнін өткен уақытпен есептеуге БОЛМАЙДЫ.** Ол
   `planned_progress()` RPC-тен келеді (CLAUDE.md §5.2). Макеттердің бәрі
   сызықтық формуланы қолданады — оларды үлгі тұтпаңыз.

## Тексеру

```
npm run typecheck                 # tsc --noEmit
npx expo export --platform ios    # бандл жиналатынын тексеру
npm start                         # Metro
```
